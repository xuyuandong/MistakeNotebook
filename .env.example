# 复制为 .env 后填写。密钥只放环境变量,不入库不入日志。

# 危险区解锁口令:设置页「永久删除全部数据」需输入此项才能执行;
# 留空(或不配置)= 清空功能锁定。公网部署前必须先恢复鉴权(AGENTS §2)。
APP_AUTH_TOKEN=

# 服务端口
PORT=8787

# 数据目录(SQLite 与附件的持久根目录)
DATA_DIR=./data

# 模型密钥(变量名与 config/models.yaml 中 api_key_env 对应)
KIMI_API_KEY=
DEEPSEEK_API_KEY=
